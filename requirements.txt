# Core HTTP and networking
requests>=2.28.0
aiohttp>=3.8.0
aiodns>=3.0.0
websockets>=10.0

# DNS and network utilities
dnspython>=2.2.0
scapy>=2.4.5
psutil>=5.9.0

# Web scraping and parsing
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Image processing and metadata
Pillow>=9.0.0

# Console and UI
rich>=12.0.0
tqdm>=4.64.0

# Security and cryptography
pyOpenSSL>=22.0.0
cryptography>=37.0.0
python-whois>=0.7.3

# Async utilities
asyncio-throttle>=1.0.2

# Additional utilities
python-dateutil>=2.8.0
urllib3>=1.26.0
certifi>=2022.0.0
charset-normalizer>=2.0.0
idna>=3.0.0

# Optional extras — install separately when needed:
# GPU:        pip install spectra-suite[gpu]   (requires CUDA/OpenCL toolkit)
# Selenium:   pip install spectra-suite[web]
# Data:       pip install spectra-suite[data]